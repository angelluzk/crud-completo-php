{
  "name": "project/crud-completo-php",
  "description": "CRUD completo em PHP com PostgreSQL no padrão MVC",
  "type": "project",
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    }
  },
  "require": {
    "ext-pdo": "*",
    "vlucas/phpdotenv": "^5.5",
    "nikic/fast-route": "^1.3",
    "psr/http-message": "^2.0",
    "nyholm/psr7": "^1.8"
  },
  "scripts": {
    "post-install-cmd": [
      "@composer dump-autoload --optimize"
    ],
    "post-update-cmd": [
      "@composer dump-autoload --optimize"
    ]
  }
}
